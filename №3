#include <iostream>

using namespace std;

int main()
{
	int N, M, K;
	cin >> N >> M >> K;

	if ((K < N * M) and (K % N == 0 or K % M == 0))
	{
		cout << "YES";
	}
	else
	{
		cout << "NO";
	}
	return 0;
}
